#include "main_view.h"
#include "asset_manager.h"

MainView::MainView(SDL_Surface* screen) {
	boundaryMainView = SDL_Rect{
		0, 0, screen->w
	}
	
};

void MainView::draw(SDL_Surface* screen) {
	
	
};

SDL_Rect MainView::boundary() const {
	
	
};

